{
	"name": "Development",
	"image": "ghcr.io/natb1/community-dashboard-development",
	"forwardPorts": [
		9876, // karma
		4200, // client dev server
		5001, // plots server
		6006 // storybook
	],
	// Mirroring the update command here because GH seems to have caching issues otherwise.
	"updateContentCommand": "npm ci && pip install -e './projects/plots/'[development] && SHELL=/bin/bash ng completion && ng analytics disable && echo \". $PWD/projects/development/env.sh\" >> ~/.bashrc",
	"hostRequirements": {
		"memory": "7gb"
  }
}
