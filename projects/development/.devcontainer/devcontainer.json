// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/alpine
{
	"name": "Development (Prebuild)",
	"build": {
		"dockerfile": "Dockerfile",
		"args": {}
	},
	"features": {
		"ghcr.io/devcontainers/features/docker-from-docker:1": {},
		"ghcr.io/devcontainers/features/git:1": {},
		"ghcr.io/devcontainers/features/git-lfs:1": {}
	},
	// "updateContentCommand": "yarn && pip install -e './projects/plots/'[development] && SHELL=/bin/bash ng completion && ng analytics disable && echo \". $PWD/projects/development/env.sh\" >> ~/.bashrc",
	"updateContentCommand": "yarn && pip install -e './projects/plots/'[development] && echo \". $PWD/projects/development/env.sh\" >> ~/.bashrc",
	"customizations": {
		"vscode": {
			"extensions": [
				"Angular.ng-template",
				"ms-python.python"
			]
		}
	}

}